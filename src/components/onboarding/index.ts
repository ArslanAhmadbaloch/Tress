/**
 * The funnel's own screens.
 *
 * Only the invitation into the scan is left here: the analysing beat,
 * the how-it-works carousel and the pre-scan profile report left the
 * funnel with its rebuild in the reference's rhythm (the reference has
 * none of them), and the example journeys moved to the Learn tab. The
 * pages the funnel is made of now come from `./kit`.
 */

export { ScanInvite, inviteHero } from './scan-invite';
